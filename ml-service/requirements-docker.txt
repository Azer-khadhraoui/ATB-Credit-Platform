# Runtime-only dependencies for the containerized ML service.
# (requirements.txt also includes jupyter/matplotlib/seaborn, needed only for
# the training notebook — not for serving predictions.)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pandas==2.2.2
numpy==1.26.4
# Must match the version the model was trained with (see notebooks/01_train_model.ipynb).
# scikit-learn warns that unpickling across versions "may lead to invalid results" —
# not something to tolerate in a scoring service.
scikit-learn==1.7.2
joblib==1.4.2
pydantic==2.8.2
