__pycache__/
*.pyc
.venv/
venv/
.env
data/processed/

# Test / coverage artifacts
.pytest_cache/
.coverage
coverage.xml

# The trained model artifacts under models/ ARE versioned on purpose: they are a
# few KB and the service cannot start without them (Docker build, CI, fresh clone).

