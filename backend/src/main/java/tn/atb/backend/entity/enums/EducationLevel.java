package tn.atb.backend.entity.enums;

/**
 * Maps to the Loan Prediction dataset's binary Education feature
 * (Graduate / Not Graduate), which the risk model consumes directly.
 */
public enum EducationLevel {
    GRADUATE,
    NOT_GRADUATE
}
